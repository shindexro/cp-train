#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

enum Rank {
    high_card,
    one_pair,
    two_pairs,
    three_of_a_kind,
    straight,
    same_suit,
    full_house,
    four_of_a_kind,
    straight_flush,
    royal_flush
};

int main()
{
    int p1_wins = 0;

    cout << p1_wins << "\n";
    return 0;
}
