#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(int _t)
{
    int a, b; cin >> a >> b;
    cout << a * b << "\n";
}

int main()
{
	ios_base::sync_with_stdio(false), cin.tie(nullptr);
	int T;
	cin >> T;
	for (int t = 1; t <= T; t++) solve(t);
}
