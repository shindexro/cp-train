#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(int t)
{
    int w;
    cin >> w;
    if (w % 2 == 1 || w == 2)
        cout << "NO";
    else
        cout << "YES";
}

int main()
{
	ios_base::sync_with_stdio(false), cin.tie(nullptr);
    // freopen("", "r", stdin);
    solve(0);
}

